# Mm annotation files are generated at Docker build time by build_mm39_reference_db.py
# Mm processed database is generated by build_mm_db.py inside the container
AltDatabase/

# Hs.bed is downloaded at build time via curl from the upstream SNAF repo
Hs.bed